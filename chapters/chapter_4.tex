
\chapter{同步语义分割和视差估计网络}
共43-53页，共10页。
头部0.5，说明2.5D分割的总体架构。
%-------------------------------------------------------------------------------3
\section{基于孪生神经网络的设计思路}
头部1，说明总体设计思路，为何是孪生网络这种形式。2.5D的分割主要指对于左图进行视差、深度估计，用右图为左图提供额外的视差训练需要的信息。
\subsection{CNN特征在不同任务下的应用}
1页，主要指出有哪些特征提取方式，本工作中特征提取是什么技术(resnet)，用到的思路，这种技术的好处(指标不低，占用小，为多任务训练留出可能)。

\subsection{端到端的多任务神经网络设计思路}
主要指出一个整体的端到端的网络流程，画出网络简要的功能模块图，指出各个模块间的关系。1页。

%-------------------------------------------------------------------------------5
\section{网络结构}
承接上文，指出网络的详细结构，同时画一个大的详细的网络结构图，指出各模块间数据的流向以及反向传播梯度传播方向、训练到的参数有哪些。1页。
\subsection{特征提取网络}
讲述详细的特征提取网络，有哪些层，重要的层进行特别说明(主要是skip connection)，指出为什么这么设计(skip connect 提供多尺度、多抽象层级的特征)。1页。
提出本文改进点：
\subsection{语义分割分支}
语义分割的详细结构图、描述。0.5页。
\subsection{特征代价卷的构建}
详述代价卷的设计，画图。0.5页。
\subsection{堆叠的沙漏3D卷积视差估计分支}
3DCNN、单个沙漏3DCNN、堆叠的沙漏3DCNN图。特征来源、回归、描述。1页。

\subsection{自相关层的提出与设计}
1页。
这里视实验情况，确定是否加入自相关层，要做实验。
目前规划：在当前最好的结果上继续加上自相关loss进行训练，如果指标再能提高一些就可以写进论文里了。

%-------------------------------------------------------------------------------2
\section{损失函数设计}
加本质小结2页。
\subsection{语义分割分支的损失函数设计}
\subsection{视差估计分支的损失函数设计}
\subsection{多任务下的损失函数的设计}
这里还需要查阅文献思考策略。          

\section{本章小结}